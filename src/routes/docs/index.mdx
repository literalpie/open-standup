import { PersonStatus } from "../../components/person-status/PersonStatus";

This is test documentation using MDX!

Here's the component:

```JSX
<PersonStatus name="Ben" done current={false} />
```

<PersonStatus name="Ben" done current={false} />
